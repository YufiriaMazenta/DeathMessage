name: DeathMessage
author: YufiriaMazenta
main: pers.yufiria.deathmsg.DeathMessage
version: ${version}
api-version: 1.19
load: POSTWORLD
soft-depend: [ PlaceholderAPI ]
folia-supported: true
dependencies:
  server:
    PlaceholderAPI:
      load: AFTER
      required: false
      join-classpath: true